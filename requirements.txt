# ============================================================
# 项目依赖文件（精简版）
# 用途：仅支持 ICL 缠论结构计算 + Binance 行情获取 + AI 调用
# 不包含 chanlun-pro 的 Web、券商接入、多市场等扩展功能
# ============================================================

# 基础数值与数据处理（Python 3.12 兼容）
numpy>=1.26.4
pandas>=2.2.2

# HTTP 请求（用于 Binance REST API 和自定义 AI 调用）
requests>=2.31.0

# 时间处理（pandas 内部也依赖，显式声明确保版本）
python-dateutil>=2.9.0

# YAML 配置文件解析
PyYAML>=6.0.1

# 环境变量管理（用于读取 .env 文件）
python-dotenv>=1.0.0

# ============================================================
# chanlun-pro 缠论引擎
# ============================================================
# 注意：chanlun-pro 不在 PyPI 上，需要从源码安装：
#   1. git clone <chanlun-pro 仓库地址>
#   2. cd chanlun-pro
#   3. pip install .
#
# 安装完上述依赖后，再执行 chanlun-pro 的源码安装即可。
# ============================================================

# ============================================================
# AI SDK（可选，如果使用官方 SDK）
# ============================================================
# 如果你只用 ai/llm.py 里基于 HTTP 的自定义封装，可以不装下面这两个：
# openai>=1.14.0
# anthropic>=0.30.0
#
# 如果你想用官方 SDK，取消上面两行的注释。
# ============================================================
